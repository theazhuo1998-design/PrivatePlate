# Third-party notices

PrivatePlate depends on third-party npm packages listed in `package-lock.json`.
Their own licenses continue to apply.

Model names in `.env.example` are configuration examples only. This repository
does not redistribute model weights. Users are responsible for reviewing the
license, acceptable-use terms, and deployment requirements of any model they run.

The optional embedding example references `BAAI/bge-small-zh-v1.5` and vLLM but
does not bundle either project or their model files.
